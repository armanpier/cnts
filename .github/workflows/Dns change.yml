name: Update DNS Address in "test.txt"

on:
  workflow_dispatch:
    inputs:
      dns_address:
        description: 'New DNS Address (e.g., tcp://1.1.1.1)'
        required: true
        default: 'tcp://8.8.8.8'

jobs:
  update-dns:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Git config
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Replace DNS Address in "test.txt"
        run: |
          if [ -f "test.txt" ]; then
            echo "Updating test.txt file..."
            perl -0777 -i -pe '
              s/"address":\s*"tcp:\/\/[^"]+"/"address": "'"${{ inputs.dns_address }}"'"/g
            ' "test.txt"
          else
            echo "File 'test.txt' not found!"
          fi

      - name: Commit and push changes
        run: |
          git add test.txt
          git diff --cached --quiet || git commit -m "Update DNS address in test.txt file to ${{ inputs.dns_address }}"
          git pull --rebase
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}